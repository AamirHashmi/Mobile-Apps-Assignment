<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobile_apps_assignment">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileAppsAssignment">
        <activity android:name=".ShoppingListDetailScreen"></activity>
        <activity android:name=".IngredientDetailScreen" />
        <activity android:name=".IngredientSearchScreen" />
        <activity android:name=".CreateShoppingListScreen" />
        <activity android:name=".ShoppingListsScreen" />
        <activity android:name=".RecipeDetailScreen" />
        <activity android:name=".SearchScreen" />
        <activity android:name=".Login" />
        <activity android:name=".LandingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MobileAppsAssignment.NoActionBar" />
    </application>

</manifest>